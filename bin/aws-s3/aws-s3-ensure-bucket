#!/usr/bin/env python3
import argh
import aws
import aws.s3

def main(name, acl='private'):
    aws.s3.ensure_bucket(name, acl)

if __name__ == '__main__':
    with aws.setup():
        argh.dispatch_command(main)
