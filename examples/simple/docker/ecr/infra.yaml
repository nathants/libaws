name: test-infraset-${uid}

lambda:
  test-lambda-${uid}:
    entrypoint: ${account}.dkr.ecr.${region}.amazonaws.com/test-container-${uid}@${digest}
    attr:
      - memory=128
      - timeout=60
    policy:
      - AWSLambdaBasicExecutionRole
    trigger:
      - type: ecr
