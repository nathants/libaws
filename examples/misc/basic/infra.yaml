name: test-infraset-${uid}

lambda:
  test-lambda-${uid}:
    entrypoint: main.py
    attr:
      - memory=128
      - timeout=60
    require:
      - -rrequirements.txt
    env:
      - uid=${uid}
    policy:
      - AWSLambdaBasicExecutionRole
